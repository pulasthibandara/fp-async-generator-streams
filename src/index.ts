import * as stream from './stream'

export {
  /**
   * Exports the streams implementation as named object so that
   * it could be used with IDEs that support smart imports.
   *
   * Implementation: {@link stream.Stream}.
   *
   * @example
   * import { stream } from 'fp-async-generator-streams'
   *
   * @since 0.1.0
   */
  stream
}
